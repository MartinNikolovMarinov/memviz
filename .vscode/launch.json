{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug memviz with LLDB",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/memviz",
            "args": [],
            "cwd": "${workspaceFolder}",
            "stopOnEntry": false,
            "env": {},
            "preLaunchTask": "Build project with Makefile",
        },
    ]
}
